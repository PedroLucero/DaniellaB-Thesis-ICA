name: daniella_tesis_app
description: "A new Flutter project."

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.1.1

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  provider: ^6.1.2
  fl_chart: ^0.67.0
  intl: ^0.19.0
  image_picker: ^1.1.2
  flutter_local_notifications: ^17.1.2
  permission_handler: ^11.3.1
  flutter_timezone: ^1.0.8
  timezone: ^0.9.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  assets:
    - images/ICAlogo.png